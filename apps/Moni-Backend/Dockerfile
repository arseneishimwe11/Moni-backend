# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build Moni-Backend`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t Moni-Backend`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system Moni-Backend && \
          adduser --system -G Moni-Backend Moni-Backend

COPY dist/apps/Moni-Backend Moni-Backend/
RUN chown -R Moni-Backend:Moni-Backend .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix Moni-Backend --omit=dev -f install

CMD [ "node", "Moni-Backend" ]
